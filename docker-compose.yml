services:
  movie-streaming:
    build: .
    container_name: movie-streaming
    restart: always
    network_mode: host
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=${PORT:-5992}
      - APP_URL=${APP_URL:-https://watch.pallabdev.in}
